#include <stdio.h>
int main() { 
    int i,j;
    int a[5];
    int b[5]={};
    printf("Enter array element:");
    for(i=0;i<5;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<5;i++)
    {
        b[i]=a[i];
        printf("%d\t",b[i]);
    }
   
    return 0;
}
